import { SetupState } from '@/lib/setup-state';

export function Step4Dashboards({ state, updateState }: { state: SetupState; updateState: (u: Partial<SetupState>) => void }) {
  return <div>Step 4 - Dashboards</div>;
}
