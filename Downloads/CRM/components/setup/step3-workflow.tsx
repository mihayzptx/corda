import { SetupState } from '@/lib/setup-state';

export function Step3Workflow({ state, updateState }: { state: SetupState; updateState: (u: Partial<SetupState>) => void }) {
  return <div>Step 3 - Workflow</div>;
}
