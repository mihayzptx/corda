import { SetupState } from '@/lib/setup-state';

export function Step1Items({ state, updateState }: { state: SetupState; updateState: (u: Partial<SetupState>) => void }) {
  return <div>Step 1 - Items</div>;
}
