import { SetupState } from '@/lib/setup-state';

export function Step2Pipeline({ state, updateState }: { state: SetupState; updateState: (u: Partial<SetupState>) => void }) {
  return <div>Step 2 - Pipeline</div>;
}
